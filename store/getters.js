export default {
    cookie: (store) => {
        return store.app.cookie
    },
	songList: (store) => {
		return store.app.songList
	},
    userInfo: (store) => {
        return store.app.userInfo
    }
}
